# Changelog

書式は Keep a Changelog に従う。日付は `YYYY-MM-DD`。

## [Unreleased]

### Added

- 作業フェーズの強制を追加し、線画→バケツ塗り→影→反射→背景の順に進めないと描けないようにした（図形ごとの `phase` とドキュメントの現在 `phase` の二重検証、配列の作業順強制、 `POST /shapes` は現フェーズのみ受付、 `POST /phase` は一段ずつの進行、 `DELETE /shapes` は線画へ復帰、DSLは `version: 2`）
- バケツ塗りを追加し、種点からのラスタ flood fill を `rect` 束（高さ1・`phase: base`）へ展開して保存できるようにした（`POST /bucket`、PNG/SVG/プレビューの描画一致を維持、同一入力は同一出力、新規依存なし）
- 背景の描画を層として最背面に固定し、作業順が最後でも表示では背面に合成されるようにした（PNG・SVG・プレビューで同一順）
- v1→v2移行手段を追加し、旧図形を線画として取り込んで線画からやり直せるようにした（`bun run src/migrate.ts -- --input <旧JSON> --output <新JSON>`、`migrateV1ToV2Document`）
- プレビュー画面に現在のフェーズと作業順の表示を追加し、図形一覧にフェーズを付けて確認できるようにした（`phase-badge`・`phase-progress` の `data-*` 目印）

- プレビューサーバに逐次追記APIを追加し、エージェントが1件ずつ命令を積み重ねて描画できるようにした（`POST /shapes` の単発・複数追記、`DELETE /shapes` の全消去、不正入力は400で既存不変）
- プレビューサーバにリファレンス画像の窓口を追加し、見本を画面表示と `/reference` 取得の両方で受け渡せるようにした（`--reference <画像パス>`）
- 受け取った命令欄を固定高さのスクロール表示にし、更新時に末尾へ自動スクロールして最新200件のみ表示するようにした
- プレビュー画面の配置と配色を見直し、狭幅画面でも破綻しない2欄グリッドと状態表示に整えた

- JSON DSL入力からPNGを出力する最小CLIを追加し、エージェントが描画と検証のループを回せるようにした
- DSL検証（色形式・寸法上限・図形種別）と日本語エラーメッセージを追加し、誤入力の原因を判断できるようにした
- 描画の退行を検出するため、PNGシグネチャとピクセル一致のテストを追加した
- 利用開始・更新・検証の文書（README、`how-to-update.md`、`verification.md`）を追加し、初期整備の手順を確認できるようにした
- ブラウザプレビューを追加し、キャンバスの自動更新と受け取った命令（図形一覧・JSON原文・検証エラー）を同時に見られるようにした
- SVG出力を追加し、画像を見られないエージェントでも描画結果をコードとして厳密に比較・検証できるようにした（CLIの `--svg` とプレビューの `/svg`、PNGと同一セマンティクス）
- ブラシ相当の自由線 `path` と図形ごとの `opacity` を追加し、ペイントソフトとして描ける幅を広げた（PNG・SVG・プレビューで同一描画）
- 負の幅・高さのrectを検証で拒否し、PNGとSVGの描画不一致を事前に防げるようにした
- 出力書込失敗とプレビュー起動失敗を日本語の案内付き終了にし、スタックトレースだけで終わらないようにした
- 描画とフィードバックのループを試せるよう、バストアップとフルサイズの初音ミク例を追加した

### Fixed

- PowerShellで逐次描画の例を実行できるよう、curl.exeに混在していたPowerShell用引数をInvoke-RestMethod / Invoke-WebRequestへ修正し、プレビューが入力JSONを更新することを復旧手順に明記した。
- 全テスト成功時も `bun test` が終了コード1で終わる問題を修正し、検証コマンドが正しく成功判定できるようにした（Bunでは `process.exitCode = undefined` で終了コードが戻らないため、失敗系テストの末尾で0に明示復帰）

### Changed

- フルサイズ初音ミク例を、一定幅の太い線ではなく塗りつぶし輪郭の髪と接続した体で描き直し、全身のキャラクターとして判別しやすくした
