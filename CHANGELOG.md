# Changelog

書式は Keep a Changelog。未リリースの変更をまとめます。

## [Unreleased]

### Added

- 複数参照からキャラクターの特徴を保ちつつ別ポーズを描く例として、バターの跳躍イラストを追加した。たれ耳・リボン・鈴・縞ソックス・ポシェット・スリングショットを曲線で描き、プレビューへ逐次送信できる。
- JSONの図形からPNG/SVGを生成するCLI、入力検証、ブラウザプレビューを追加し、コードで描画して結果を確認できるようにした。
- リファレンスへ近づけながら部分修正できるよう、v3 DSLにネイティブベジェ曲線、線形・放射グラデーション、クリッピング、変形、レイヤー、図形IDとグループ、非表示を追加した。
- 線画や目だけを描き直せるよう、図形・グループの部分更新と削除、全体置換、Undo/Redoを追加した。編集は検証後に直列化・原子的保存し、削除した自動IDを新しい図形へ再利用しない。
- 参照画像の表示・切り抜き・並列/半透明/差分比較を追加し、位置や輪郭のずれを確認できるようにした。参照を作品の書き出しへ混ぜない。
- 細部の比較に使えるよう、元図形から再描画する局所拡大PNGをCLIのcrop/scaleとプレビューAPIへ追加した。
- 連続した領域を塗れるバケツ塗りを追加し、高さ1のrect束として保存することでPNG/SVGの領域を一致させた。
- 曲線・色から精密描画を再現できる初音ミクの胸像サンプルと、プレビューへ逐次送信するライブ描画を追加した。
- 描画基盤・旧形式の見た目保存・編集履歴・ID再利用防止・局所出力・比較設定・ビルドしたCLIの起動の回帰テストを追加した。
- 利用開始・更新・復旧・検証・貢献・サポートの文書を整備し、操作と保守の手順を確認できるようにした。

### Changed

- 反復修正を妨げないよう、phaseを作業ガイドへ変更し、工程を戻る・飛ばす・別工程の図形を修正する操作を可能にした。旧v1/v2は見た目を保つ明示的移行の対象とした。
- 作品と参照の比較に集中できるよう、プレビューをダークカラーの制作スタジオへ刷新し、比較ステージと参照・レイヤー・書き出しのインスペクターへ整理した。
- 拡大操作で細部がぼやけにくいよう、表示倍率に合わせてCanvasの描画解像度も増加させた。全体fitは画面サイズ変更へ追従する。
- 図形検索は全件を対象にし、表示は最新200件へ制限して大量の命令でも画面を操作しやすくした。

### Fixed

- バターのバランス修正後に頬を隠していた前髪を短く整理し、下膨れの輪郭を見せた。頭部圧縮で丸くなった瞳を楕円へ補正し、耳先と付け根も丸くつないだ。
- バターの顔が縦長で頭身が参照と異なる問題を修正し、頬で横に膨らむ輪郭・大きな頬・小さめの頭と幅のある胴へ調整した。ポーズの接続を保つため、腕・脚・バッグの配置も合わせた。
- 首と襟の間に背景が見える問題を修正し、首の輪郭と陰影のマスクを襟の下まで重ねた。
- まつ毛の本体とハネの間に隙間が出ないよう、左右それぞれを一続きの輪郭へ統合した。
- 透明度を含む描画でPNG/SVGが対応するよう、SVGのグラデーションalphaと塗り・線の不透明度を明示化した。
- 極端な有限座標がネイティブ描画例外を起こさないよう、幾何数値を描画可能な範囲で検証するようにした。
- 旧形式で無視された項目が移行後に有効にならないよう、既知の旧図形属性だけを引き継ぐようにした。
- 削除後の自動ID再利用により古い選択が別部品を指す問題を防いだ。
- 局所PNGのクライアントとAPIでquery契約をそろえ、画面からの取得が拒否される問題を修正した。
- ビルド後のJavaScriptファイルでもCLI・移行・プレビューが直接起動するよう、エントリーポイント判定を修正した。
- PowerShellでAPI例を実行できるよう、curl.exeに混在していたPowerShell用引数をInvoke-RestMethod / Invoke-WebRequestへ修正した。
- 全テスト成功時も終了コード1になる問題を修正し、Bunの失敗系テスト後に0を明示して復帰するようにした。
