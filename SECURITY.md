# セキュリティ方針

## 対象

- 本リポジトリのソースコード、設定、ドキュメント、依存関係。

## 報告

脆弱性の疑いを見つけた場合は、公開のIssueではなく、再現手順・影響範囲・確認したバージョンを添えてメンテナーへ非公開で連絡する。
概念実証コードに秘密情報や個人情報を含めない。

## 対応

- APIキー、トークン、Cookie、認証情報、個人情報をコミットしない。
- `.env`、設定ファイル、ログ、スクリーンショットに秘密情報が含まれないか確認する。
- 依存関係を変更した場合は `bun run build` と `bun test` に加え、脆弱性チェックが定義されていれば実行する。
