# 検証手順

## 自動検証

```powershell
bun run lint
bun run format
bun run type-check
bun run build
bun run test
```

- lint: `any`禁止、型付きESLint、警告0件。描画サンプルのTypeScriptも対象。
- format: ソース・テスト・サンプル・設定のPrettier検査。
- type-check: strict TypeScript。ブラウザの比較・編集コードも型検査。
- build: CLI・プレビュー・移行CLIをBun向けにビルド。
- test: DSL、描画、移行、CLI、API、編集履歴、比較設定の単体・結合テスト。

主要な回帰観点:

| 対象 | 確認内容 |
| --- | --- |
| DSL | 旧版拒否、欠落・無効なphase、有限数値・上限、色・gradient stops、曲線命令、ID重複、マスク・変形 |
| 描画 | 曲線、線形／放射gradient、alpha、clip、transform、非表示、明示layer優先、安定順、PNG/SVGの内部画素対応 |
| 局所出力 | キャンバス範囲・倍率・4096px上限、曲線からの再描画、CLI crop／scaleとSVG全体出力 |
| 移行 | v1/v2の見た目保存、旧無視属性を有効化しない、旧形式の未知図形・gradient拒否 |
| バケツ | RGBA連続領域、許容差、同色・領域外・上限、共通rendererと一致、工程を問わず実行 |
| 編集 | 単発／複数追記、ID／グループPATCH・DELETE、任意項目null除去、全文検証、不正時入力不変 |
| 保存・履歴 | concurrent追記の保持、atomic保存、undo/redo、新規編集でredo消去、外部保存で履歴reset、自動ID再利用防止 |
| API | 参照200/404、SVG/PNG、別Origin書込拒否、未知query拒否、壊れたJSONからの復帰 |
| 比較設定 | crop範囲・縦横比、設定復元、fit計算、手動zoomと高解像度backing、局所PNG URLと実APIの接続 |

## 実ブラウザでの確認

参照画像を指定してプレビューを起動し、1920×1080、実際のアプリ内表示幅、390×844で確認します。要素特定には `data-testid` を使います。

- 作品と参照が全体fitで見え、細い線・色・寸法が書き出しと対応する。
- 参照の切り抜きを適用・解除し、並列／半透明／差分／作品のみを切り替えられる。
- 不正cropにエラーが出て、有効な表示設定を壊さない。
- zoom・fit・枠内スクロールと座標表示が一致し、リサイズでも横方向へ画面全体がはみ出さない。
- 再読み込み後に比較設定が復帰する。参照未設定でも描画・編集が動く。
- 図形検索→選択→JSON部分修正→表示反映→Undo/Redoを操作する。
- グループの変形・非表示・削除と復元を操作する。
- 工程を前後に変更しても絵が消えず、追加や修正を続けられる。
- 局所PNGを画面から取得でき、リンク・画像の寸法が指定した範囲と倍率に合う。
- 無効なJSON編集が拒否され、コンソールの未処理例外がない。
- インスペクターの切り替えや開閉領域がキーボードでも操作できる。

## 出力の確認

```powershell
bun run src/cli.ts -- --input examples/hello.json --output out/hello.png --svg out/hello.svg
bun run examples/miku-study.ts
```

PNGの先頭8バイトは `137,80,78,71,13,10,26,10`。同一入力からのSVGが決定的で、CLIと `/svg` が一致することを確認します。精密描画サンプルは作品に参照画素を含めず、曲線・色だけで再生成できることも確認します。

## 実施記録

2026-09-08: v2による初回の初音ミク逐次描画は、全5工程、1000×1100・2222図形、参照表示、PNG/SVG出力を確認。初回成果物は `out/miku-wind/` に保存（Git管理外）。

2026-09-08（v3）: lint・format・type-check・buildが成功。96テスト・486検証が成功。900×1080・203図形（まつ毛の輪郭統合後）の精密描画を再生し、首と襟の隙間と、まつ毛のハネの隙間を修正。ソース生成PNGとライブ入力PNGの完全一致、初回v2作品の移行前後PNGの完全一致を確認した。2700×3240の全体PNGと1200×1200の顔の局所PNGを出力した。

実ブラウザでは、1920×1080・アプリ内約788px・390×844で比較のfitと画面幅への収まりを確認。参照cropの適用・不正値拒否・表示リセットと再読み込み後の復帰、比較4モード、200%で1800×2160への描画解像度増加、図形の不正/正常PATCH・削除・Undo/Redo、34図形のグループ移動・削除・復元、工程の戻りと再変更、局所PNG取得、タブの左右/Home/Endキーを操作した。ビルド版のプレビューも起動し、参照なしで描画・未設定表示・コンソールエラーなしを確認した。

ピクセル単位の参照画像一致や、任意のキャラクター・ポーズでの再現精度を保証するものではない。比較画像は out/miku-study/comparison.png、作品と局所出力は同じフォルダへ保存（Git管理外）。

2026-09-08（バター）: 3枚の参照を比較用シートにまとめ、別ポーズ・表情・背景の全身イラストを346図形で描画した。元画像は作品へ埋め込まず、曲線・色・クリップ・変形のみ使用。1280×1280のPNG/SVG/JSON、3840×3840の拡大PNG、顔・衣装の局所PNGを保存した。ライブ入力と生成元のPNG一致、APIと保存SVGの一致、実ブラウザで参照シートと作品の表示、まつ毛・襟・手・小物の接続を確認。lint・format・型検査・build・96件のテストが成功した。成果物は `out/butter-adventure/`、再生成元は `examples/butter-adventure.ts`。

同日のバランス修正: 顔を頬で広がる輪郭へ変更し、頬を拡大。首位置を基準に頭部を横0.86・縦0.70へ調整し、胴は横1.24・縦1.12として身体との比率を修正した。関連する腕・脚・バッグの位置を合わせ、背景とキャラクターの配置も調整。プレビューで全体を比較し、顔・衣装の拡大出力と全品質チェック・96テストの成功を確認した。修正前は `before-proportions.png` とJSONに保存。

続きのリファイン: 頭身を維持して、頬にかかる髪の輪郭と折り返しを整理。瞳・まつ毛・ハイライトを一緒に縦1.16で局所補正し、耳先・付け根を丸く調整した。880×520の顔拡大と全体像を確認し、ライブPNG/SVGと保存データの一致、lint・format・型検査・build・96テスト成功を確認。再開時に停止していたプレビューを起動し直した。最新版の通常PNGは `out/butter-adventure/butter-refined.png`。

設定動画による再調整: 333秒の動画を28枚の一覧、15場面の原寸フレーム、6時刻の連続動作で確認。衣装・牙・反射点・しっぽ・パチンコを修正し、344図形で再出力した。原寸・3840×3840・顔880×520・衣装1210×1010を保存し、実ブラウザで動画の全身資料と並列表示を確認。lint・format・型検査・build・96テスト成功。分析は `docs/butter-video-study.md`、改訂PNGは `out/butter-adventure/butter-video-refined.png`。音声の逐語書き起こしは未実施。

ライフセーバーの別作品: 8候補から1:50の笑顔を選び、別ソース `examples/butter-lifeguard.ts` に253図形で描いた。選択フレームとの重ね表示で顔・腕の位置を確認し、目元と口は3倍の局所画像で接続を確認。1000×1120、3000×3360、顔1125×600のPNGとSVG/JSONを `out/butter-lifeguard/` に保存。lint・format・型検査・build・96テストが成功。隠れたサンダルは1:10の同衣装を参考に補完した。
